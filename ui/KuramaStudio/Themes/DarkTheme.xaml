<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- Kurama palette: charcoal + foxfire amber -->
  <Color x:Key="BgDeep">#FF0B0C10</Color>
  <Color x:Key="BgPanel">#FF12141A</Color>
  <Color x:Key="BgElevated">#FF1A1D26</Color>
  <Color x:Key="BgHover">#FF222633</Color>
  <Color x:Key="BgInput">#FF161922</Color>
  <Color x:Key="BorderSubtle">#FF2A2F3A</Color>
  <Color x:Key="BorderStrong">#FF3A4150</Color>
  <Color x:Key="TextPrimary">#FFF3F4F6</Color>
  <Color x:Key="TextSecondary">#FF9CA3AF</Color>
  <Color x:Key="TextMuted">#FF6B7280</Color>
  <Color x:Key="Accent">#FFF59E0B</Color>
  <Color x:Key="AccentSoft">#33F59E0B</Color>
  <Color x:Key="AccentHover">#FFFBBF24</Color>
  <Color x:Key="Success">#FF34D399</Color>
  <Color x:Key="Danger">#FFF87171</Color>
  <Color x:Key="Info">#FF60A5FA</Color>

  <SolidColorBrush x:Key="BrushBgDeep" Color="{StaticResource BgDeep}"/>
  <SolidColorBrush x:Key="BrushBgPanel" Color="{StaticResource BgPanel}"/>
  <SolidColorBrush x:Key="BrushBgElevated" Color="{StaticResource BgElevated}"/>
  <SolidColorBrush x:Key="BrushBgHover" Color="{StaticResource BgHover}"/>
  <SolidColorBrush x:Key="BrushBgInput" Color="{StaticResource BgInput}"/>
  <SolidColorBrush x:Key="BrushBorderSubtle" Color="{StaticResource BorderSubtle}"/>
  <SolidColorBrush x:Key="BrushBorderStrong" Color="{StaticResource BorderStrong}"/>
  <SolidColorBrush x:Key="BrushTextPrimary" Color="{StaticResource TextPrimary}"/>
  <SolidColorBrush x:Key="BrushTextSecondary" Color="{StaticResource TextSecondary}"/>
  <SolidColorBrush x:Key="BrushTextMuted" Color="{StaticResource TextMuted}"/>
  <SolidColorBrush x:Key="BrushAccent" Color="{StaticResource Accent}"/>
  <SolidColorBrush x:Key="BrushAccentSoft" Color="{StaticResource AccentSoft}"/>
  <SolidColorBrush x:Key="BrushAccentHover" Color="{StaticResource AccentHover}"/>
  <SolidColorBrush x:Key="BrushSuccess" Color="{StaticResource Success}"/>
  <SolidColorBrush x:Key="BrushDanger" Color="{StaticResource Danger}"/>
  <SolidColorBrush x:Key="BrushInfo" Color="{StaticResource Info}"/>

  <FontFamily x:Key="UiFont">Segoe UI Variable Display, Segoe UI</FontFamily>
  <FontFamily x:Key="MonoFont">Cascadia Code, Consolas</FontFamily>

  <Style x:Key="NavButtonStyle" TargetType="RadioButton">
    <Setter Property="Foreground" Value="{StaticResource BrushTextSecondary}"/>
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Padding" Value="14,11"/>
    <Setter Property="Margin" Value="8,2"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
    <Setter Property="FontSize" Value="13.5"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="RadioButton">
          <Border x:Name="Bd" Background="{TemplateBinding Background}"
                  CornerRadius="10" Padding="{TemplateBinding Padding}">
            <StackPanel Orientation="Horizontal">
              <TextBlock x:Name="Icon" Text="{TemplateBinding Tag}" FontSize="15"
                         Margin="0,0,10,0" VerticalAlignment="Center"
                         Foreground="{TemplateBinding Foreground}"/>
              <ContentPresenter VerticalAlignment="Center"/>
            </StackPanel>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{StaticResource BrushBgHover}"/>
              <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{StaticResource BrushAccentSoft}"/>
              <Setter Property="Foreground" Value="{StaticResource BrushAccent}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="CardStyle" TargetType="Border">
    <Setter Property="Background" Value="{StaticResource BrushBgElevated}"/>
    <Setter Property="BorderBrush" Value="{StaticResource BrushBorderSubtle}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="14"/>
    <Setter Property="Padding" Value="18"/>
  </Style>

  <Style x:Key="SectionTitle" TargetType="TextBlock">
    <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}"/>
    <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
    <Setter Property="FontSize" Value="16"/>
    <Setter Property="FontWeight" Value="SemiBold"/>
    <Setter Property="Margin" Value="0,0,0,4"/>
  </Style>

  <Style x:Key="SectionHint" TargetType="TextBlock">
    <Setter Property="Foreground" Value="{StaticResource BrushTextMuted}"/>
    <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="TextWrapping" Value="Wrap"/>
    <Setter Property="Margin" Value="0,0,0,14"/>
  </Style>

  <Style x:Key="FieldLabel" TargetType="TextBlock">
    <Setter Property="Foreground" Value="{StaticResource BrushTextSecondary}"/>
    <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Margin" Value="0,0,0,6"/>
  </Style>

  <Style x:Key="KuramaTextBox" TargetType="TextBox">
    <Setter Property="Background" Value="{StaticResource BrushBgInput}"/>
    <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}"/>
    <Setter Property="BorderBrush" Value="{StaticResource BrushBorderSubtle}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="12,10"/>
    <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
    <Setter Property="FontSize" Value="13"/>
    <Setter Property="CaretBrush" Value="{StaticResource BrushAccent}"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TextBox">
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="10" Padding="{TemplateBinding Padding}">
            <ScrollViewer x:Name="PART_ContentHost"/>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="KuramaCombo" TargetType="ComboBox">
    <Setter Property="Background" Value="{StaticResource BrushBgInput}"/>
    <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}"/>
    <Setter Property="BorderBrush" Value="{StaticResource BrushBorderSubtle}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="10,8"/>
    <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
    <Setter Property="FontSize" Value="13"/>
  </Style>

  <Style x:Key="PrimaryButton" TargetType="Button">
    <Setter Property="Foreground" Value="#FF111827"/>
    <Setter Property="Background" Value="{StaticResource BrushAccent}"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Padding" Value="16,10"/>
    <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
    <Setter Property="FontWeight" Value="SemiBold"/>
    <Setter Property="FontSize" Value="13"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="10"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{StaticResource BrushAccentHover}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Bd" Property="Opacity" Value="0.45"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="GhostButton" TargetType="Button">
    <Setter Property="Foreground" Value="{StaticResource BrushTextSecondary}"/>
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderBrush" Value="{StaticResource BrushBorderSubtle}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="14,9"/>
    <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
    <Setter Property="FontSize" Value="12.5"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Bd" Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="10" Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{StaticResource BrushBgHover}"/>
              <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="ToggleSwitch" TargetType="CheckBox">
    <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}"/>
    <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
    <Setter Property="FontSize" Value="13"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="CheckBox">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,16,0">
              <ContentPresenter/>
              <TextBlock x:Name="Hint" Text="{TemplateBinding Tag}"
                         Foreground="{StaticResource BrushTextMuted}"
                         FontSize="11.5" TextWrapping="Wrap" Margin="0,3,0,0"/>
            </StackPanel>
            <Border x:Name="Track" Grid.Column="1" Width="44" Height="24"
                    CornerRadius="12" Background="#FF374151" VerticalAlignment="Center">
              <Ellipse x:Name="Thumb" Width="18" Height="18" Fill="White"
                       HorizontalAlignment="Left" Margin="3,0,0,0"/>
            </Border>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Track" Property="Background" Value="{StaticResource BrushAccent}"/>
              <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
              <Setter TargetName="Thumb" Property="Margin" Value="0,0,3,0"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="KuramaSlider" TargetType="Slider">
    <Setter Property="Minimum" Value="0"/>
    <Setter Property="IsSnapToTickEnabled" Value="True"/>
    <Setter Property="Foreground" Value="{StaticResource BrushAccent}"/>
  </Style>

</ResourceDictionary>
